# Generated by PatchCourt.
# Review this file before using it in CI.
# Preset: go-clean

ignore:
  paths:
    - ".git/**"
    - "build/**"
    - "cmake-build-debug/**"
    - "cmake-build-release/**"
    - "node_modules/**"
    - "vendor/**"
    - "libs/**"
    - "third_party/**"
    - "external/**"
    - "generated/**"
    - "**/*.pb.h"
    - "**/*.pb.cc"
    - "**/*.pb.cpp"
    - "**/*.pb.go"
    - "**/*.grpc.pb.h"
    - "**/*.grpc.pb.cc"
    - "**/*.grpc.pb.go"

cpp:
  compile_commands:
    auto_discover: true

layers:
  cmd:
    paths:
      - "cmd/**"
    may_depend_on:
      - cli

  cli:
    paths:
      - "internal/cli/**"
    may_depend_on:
      - app
      - output

  app:
    paths:
      - "internal/app/**"
    may_depend_on:
      - analysis
      - changes
      - config
      - model
      - platform
      - reportmodel

  changes:
    paths:
      - "internal/changes/**"
    may_depend_on:
      - analysis
      - model
      - platform

  analysis:
    paths:
      - "internal/analysis/**"
    may_depend_on:
      - config
      - model
      - platform

  config:
    paths:
      - "internal/config/**"
    may_depend_on:
      - model

  model:
    paths:
      - "internal/model/**"
    may_depend_on: []

  reportmodel:
    paths:
      - "internal/reportmodel/**"
    may_depend_on:
      - analysis
      - config
      - model

  output:
    paths:
      - "internal/output/**"
    may_depend_on:
      - analysis
      - model
      - reportmodel

  platform:
    paths:
      - "internal/platform/**"
    may_depend_on: []

